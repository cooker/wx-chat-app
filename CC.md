下面给你一份基于设计稿（移动端相册首页）整理的**研发改造需求稿**，偏技术落地与验收。

## 一、改造目标

将当前 `Feed` 页改造为设计稿风格的移动端首页，提升首屏质感、内容发现效率与交互流畅度。  
核心目标：

- 首页信息架构从“纯瀑布流”升级为“搜索 + 热门横滑 + 推荐瀑布流”
- 视觉与交互贴近 iOS 风格（圆角卡片、轻量阴影、留白节奏）
- 保持现有分页加载能力，并优化手机端上拉加载体验
- 改造后不影响现有相册详情预览、接口鉴权与基础性能

## 二、改造范围

**包含：**

- `client/src/views/FeedView.vue` 页面结构重组
- `client/src/modules/feed/components/*` 新增/改造子组件（搜索栏、热门区、推荐卡片头部）
- `client/src/modules/feed/styles/*` 样式体系升级（移动端优先）
- 前端数据聚合层（热门、推荐、换一换、搜索关键字状态）

**不包含：**

- 新的发布/上传流程（底部 + 按钮仅保留占位交互可选）
- 管理端配置后台改造
- 推荐算法深度重构（先按规则推荐）

## 三、页面信息架构（按设计稿）

1. 顶部区  
   - 标题「相册」+ 副标题文案  
   - 右上角操作入口（如记录/草稿/消息，先支持单入口）

2. 搜索区  
   - 占位文案：`搜索相册、地点、人物...`
   - 支持点击进入搜索结果页（或本页筛选，二选一实现）

3. 热门相册区（横向）  
   - 标题「热门相册」+「查看全部」
   - 横向卡片：封面图 + 标题 + 张数（如 `1289张`）

4. 推荐区（瀑布流）  
   - 标题「推荐」+「换一换」
   - 双列瀑布流卡片：图片、标题、作者、点赞数
   - 延续现有上拉分页加载

5. 底部悬浮主操作（可选开关）  
   - 圆形 `+` 浮动按钮，默认跳转上传/新建入口（若后端未就绪可先 toast）

## 四、功能需求（研发视角）

- **F1 首屏数据并发加载**  
  首屏并发请求：热门列表、推荐列表（第一页）；失败互不阻塞。
- **F2 热门横向滚动**  
  支持手势滑动；图片懒加载；点击进入相册详情。
- **F3 推荐瀑布流**  
  沿用当前虚拟瀑布流能力；保留骨架屏、空态、错误态。
- **F4 换一换**  
  点击后刷新推荐源（可重置分页或切换推荐批次标识）。
- **F5 搜索入口**  
  点击搜索框进入搜索页；带回关键词后可回填展示。
- **F6 上拉加载更多（移动端优化）**  
  保留 IO + scroll 双通道触发，触控端阈值更早触发；避免重复请求。
- **F7 下拉刷新**  
  与上拉加载互斥，刷新时不清空已渲染列表导致闪屏。
- **F8 卡片交互**  
  点击卡片进入预览；点赞入口仅做前端态可选（若接口未开放）。

## 五、接口与数据需求

建议最小接口集合：

- `GET /api/feed/hot?page=1&pageSize=10`（热门横滑）
- `GET /api/feed/recommend?page=1&pageSize=20&batch=<optional>`（推荐瀑布）
- `GET /api/feed/search?q=xxx&page=1&pageSize=20`（搜索结果）
- `POST /api/feed/recommend/refresh` 或前端生成 `batch` 参数实现“换一换”

字段最小集：

- `id`, `title`, `coverUrl`, `imageWidth`, `imageHeight`
- `authorName`, `authorAvatar`
- `photoCount`（热门区）
- `likeCount`, `liked`（推荐区可选）

## 六、UI/交互规范（关键约束）

- 移动端基准宽度：`375`
- 卡片圆角：`12~16px`，按钮圆角：`10~22px`
- 触控点击热区最小 `44px`
- 列表滚动保持 60fps，滚动监听统一走 `rAF` 节流
- 安全区适配：底部按钮与状态区考虑 `env(safe-area-inset-bottom)`
- 图片加载失败有兜底图；文案超长两行截断

## 七、验收标准（可测试）

- **A1 结构一致性**：页面包含设计稿 4 大区块（顶部/搜索/热门/推荐）
- **A2 热门区**：横向滑动顺畅，点击可进入详情
- **A3 推荐区**：首屏渲染、上拉分页、换一换均可用
- **A4 移动端加载**：iOS Safari 与 Android Chrome 上拉均能稳定触发加载更多
- **A5 异常态**：接口失败时展示错误提示并支持重试
- **A6 性能**：首屏可交互时间与现网相比不劣化（±10% 内）

## 八、实施拆分建议（两迭代）

- **迭代 1（结构与数据）**  
  页面骨架、热门/推荐接口接入、基础样式、分页与刷新联调。
- **迭代 2（体验完善）**  
  换一换、搜索联动、细节动效、安全区与真机兼容性优化。

---

如果你要，我可以下一步直接给出这份需求稿对应的**技术任务清单（按文件粒度）**，例如具体改哪些组件、每个文件改动点和预估工时。